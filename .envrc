# Bazelle Development Environment
#
# This file is managed by direnv (https://direnv.net/).
# Run `direnv allow` to activate the environment.
#
# For full IDE integration with Bazel-managed tools, run:
#   bazel run //:bazel_env
# And follow the instructions to update this file.

# Use bazel_env.bzl generated environment (if available)
if [[ -f .bazel_env/bazel_env.sh ]]; then
    source .bazel_env/bazel_env.sh
fi

# GOPACKAGESDRIVER for gopls/IDE support with Bazel
# This allows IDEs to understand Bazel-managed Go dependencies
export GOPACKAGESDRIVER="${PWD}/tools/gopackagesdriver.cmd"
