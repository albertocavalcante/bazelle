diff --git a/MODULE.bazel b/MODULE.bazel
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -49,7 +49,10 @@ use_repo(
 
 # There is a `grpc_java` bazel_dep we could use, but that pollutes the main
 # `maven` namespace and patches `protobuf` too. Instead, we pull the http
-# archive and patch it so we can use it in the one target we care about
+# archive and patch it so we can use it in the one target we care about.
+# Bazelle: grpc-java.patch includes ProtoInfo fix for Bazel 9 compatibility
+# (ProtoInfo must be loaded from @rules_proto, not built-in)
+# See: https://github.com/grpc/grpc-java/issues/12315
 http_archive = use_repo_rule("@bazel_tools//tools/build_defs/repo:http.bzl", "http_archive")
 
 http_archive(
diff --git a/third_party/grpc-java.patch b/third_party/grpc-java.patch
--- a/third_party/grpc-java.patch
+++ b/third_party/grpc-java.patch
@@ -1,3 +1,17 @@
+# Bazelle: ProtoInfo fix for Bazel 9 compatibility
+# See: https://github.com/grpc/grpc-java/issues/12315
+diff --git a/java_grpc_library.bzl b/java_grpc_library.bzl
+--- a/java_grpc_library.bzl
++++ b/java_grpc_library.bzl
+@@ -1,4 +1,9 @@
++# Bazelle: Load ProtoInfo for Bazel 9 compatibility
++# See: https://github.com/grpc/grpc-java/issues/12315
++load("@rules_proto//proto:defs.bzl", "ProtoInfo")
++
+ """Build rule for java_grpc_library."""
+ 
+ load("@rules_java//java:defs.bzl", "JavaInfo", "JavaPluginInfo", "java_common")
+
 diff --git a/api/BUILD.bazel b/api/BUILD.bazel
 index 965d14a9e..11e3fd6d5 100644
 --- a/api/BUILD.bazel
